1. Store location of secret place
2. Have a button to track current location
3. render something on the page if location is within x distance(measure how far and how accurate I can get with turf.js and navigator.geolocation)